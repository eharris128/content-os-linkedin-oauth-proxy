{
  "name": "oauth-proxy",
  "version": "1.0.0",
  "description": "Minimal oauth proxy for LinkedIn login on Cloudflare worker",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "linkedin-api-client": "^0.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.20.3"
  },
  "engines": {
    "node": ">=16"
  }
}