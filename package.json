{
  "name": "oauth-proxy",
  "version": "1.0.0",
  "description": "Stateless OAuth proxy service for Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "setup-secrets": "node setup-secrets.js"
  },
  "dependencies": {
    "linkedin-api-client": "^0.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.20.3"
  },
  "engines": {
    "node": ">=16"
  }
}